{ Overwritten by build.sh when a release is cut.  A hand-built copy keeps
  this, which is deliberately older than any real tag so such a copy never
  claims to be up to date when it is not. }
const
  APP_VERSION = 'v0.0.0-dev';
