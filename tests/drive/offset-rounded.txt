# A rounded rectangle offset inward past its corners' radius, then pushed.
#
# From a note, 17 September: "if i tried to offset it inside it looked like it was
# flipping the inner rounded corners the wrong way when offset to the
# inside", and push/pull "wasnt detecting faces".  Each piece of a rounded
# corner used to turn round when taken in further than the radius, leaving an
# outline that crossed itself - and an outline that crosses itself encloses
# nothing push/pull can find.
#
# What to look for: hsk-or-rounded is the rectangle with four round corners;
# hsk-or-offset has a second outline inside it with square corners and no
# loops; hsk-or-ring has the ring between them pulled up; hsk-or-middle has
# the middle pulled up as well, higher.
activate
wait 650
key slash
type plan
key return
wait 650
key slash
type clear
key return
wait 650

key r
wait 300
at 400 250
click
at 800 550 500
click
wait 450

# round the bottom-left corner, then double-click the other three
key a
wait 400
at 440 550 400
wait 300
click
wait 300
at 400 500 400
wait 300
click
wait 300
at 425 530 300
at 412 538 300
wait 450
click
click
wait 650
at 797 253 400
wait 300
click
click
wait 650
at 797 547 400
wait 300
click
click
wait 650
at 403 253 400
wait 300
click
click
wait 650
key escape
wait 300
at 900 400 300
wait 400
shot /tmp/hsk-or-rounded.png

# offset: pick the face, move well inside - further in than the corners are
# round - and click
key slash
type offset
key return
wait 400
at 600 400 300
wait 400
click
wait 300
at 480 330 500
wait 450
click
wait 650
at 900 400 300
wait 400
shot /tmp/hsk-or-offset.png

# look at it from the corner, and pull the ring up, then the middle higher
key slash
type iso
key return
wait 800
key p
wait 300
at 900 400 300
wait 300

click
wait 300
at 900 330 400
wait 300
type 3"
key return
wait 650
shot /tmp/hsk-or-ring.png
at 720 230 300
wait 400
click
wait 300
at 720 150 400
wait 300
type 6"
key return
wait 650
at 1000 600 300
wait 400
shot /tmp/hsk-or-middle.png
